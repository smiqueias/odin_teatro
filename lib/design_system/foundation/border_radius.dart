import 'package:flutter/material.dart';

abstract final class OdinBorderRadius {
  static const BorderRadius circular_radius_8 = BorderRadius.all(
    Radius.circular(8),
  );
  static const BorderRadius circular_radius_10 = BorderRadius.all(
    Radius.circular(10),
  );
  static const BorderRadius circular_radius_12 = BorderRadius.all(
    Radius.circular(12),
  );
  static const BorderRadius circular_radius_16 = BorderRadius.all(
    Radius.circular(16),
  );
  static const BorderRadius circular_radius_20 = BorderRadius.all(
    Radius.circular(20),
  );
  static const BorderRadius circular_radius_32 = BorderRadius.all(
    Radius.circular(32),
  );
}
